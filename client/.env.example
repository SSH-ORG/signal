# Base URL of the FastAPI backend. Defaults to http://localhost:8000 in code
# if this isn't set, so this file only matters when running against a
# non-default backend URL.
VITE_API_URL=http://localhost:8000

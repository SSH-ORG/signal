import AuthPage from './pages/AuthPage'

// Only the auth page exists so far — routing and the real dashboard come
// once login is wired up end-to-end.
function App() {
  return <AuthPage />
}

export default App
